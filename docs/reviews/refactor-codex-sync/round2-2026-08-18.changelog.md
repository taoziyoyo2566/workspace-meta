# Round 2 Changelog: Portable Python Discovery

## Plan

- Plan: `plan-python-discovery-portability-2026-08-18.md`
- Date: 2026-08-18
- Status: implementation complete; current-host hook drift remains host-local

## Outcome

Workspace-meta now selects a Python 3.11+ interpreter through one resolver,
returns a stable path for PATH-based candidates, and embeds a shell-quoted
interpreter in generated Claude/Codex SessionStart commands. macOS system
Python 3.9 no longer shadows an available Homebrew interpreter in the managed
workflow.

## Changed Artifacts

- Added `scripts/find_python.sh` to the tracked workspace tooling surface.
- Updated `Makefile` and `bootstrap-local.sh` to use the resolver.
- Updated `sync_codex_config.py` to quote the selected interpreter in hooks.
- Updated regression tests to use `sys.executable` and cover PATH resolution
  plus interpreter paths containing spaces.
- Updated living verification and onboarding instructions to use the resolver.

## Verification

Passed:

- `make test`: 31/31 tests.
- Focused discovery and shell-quoting tests.
- `bash -n scripts/*.sh .githooks/pre-commit`.
- Resolved-Python `py_compile` for scripts and tests.
- Generated Hook shell syntax check.
- Quoted-path Hook functional smoke with an isolated HOME.
- `git diff --check`.
- `.githooks/pre-commit`.
- Isolated bootstrap twice: managed-file hashes unchanged on the second run.

Not counted as passing:

- Current-host `make agent-sync-check` reports Codex hook-state normalization
  drift and was intentionally not written.
- Real Codex/Claude UI smoke test was not run.
- No Git staging, commit, or push was performed.

## Boundary

This round does not add a Windows-native launcher or change hook ownership,
migration behavior, or host-local trust state.
