# Round 3 Changelog: Git transaction review and execution UX

## Plan

- Plan: `plan-git-transaction-ux-2026-07-20.md`
- Date: 2026-07-20
- Status: implemented; verification results recorded below

## File-change manifest

- `.agents/host-templates/codex-AGENTS.md`
  - synchronized the installed branch/worktree, stash, and integration safety
    boundaries back into the managed source;
  - replaced per-operation commit/push confirmation with result review followed
    by one complete publication command bundle;
  - made ordinary-language agent execution and operator self-execution plus
    read-only verification equally valid.
- `README.md`
  - replaced the W-R30-only design summary with the W-R31 refinement.
- `docs/architecture/codex-config-management.md`
  - documented the two checkpoints, both executor paths, drift behavior, and
    operations that remain separate.
- `feedback-register.md`
  - added W-R31 with the Saberu recurrence and the positive transaction recipe.
- `tests/test_sync_codex_config.py`
  - added regression coverage for required command-bundle semantics.
- `docs/reviews/refactor-codex-permissions/`
  - added the approved plan and this round changelog.

## Intent

Preserve W-R30's protection against publishing unreviewed content while
eliminating fixed-wording confirmation and commit/push/PR round-trip
fragmentation. A reviewed result and a reviewed command sequence are the safety
boundaries; the number of conversational turns is not.

## Explicitly not done

- No host-local `~/.codex/rules/*.rules` change.
- No merge or destructive-operation bundling.
- No Saberu project-rule change in this repository.
- No Saberu rename implementation.
- No branch/worktree cleanup.

## Verification

- **Direction**: complete diff reviewed against the approved plan and W-R30.
  Only the seven declared workspace-meta paths are changed; no host-local
  authorization file or pre-existing dirty workspace content is included.
- **Static**:
  - `git diff --check`: passed.
  - `bash -n scripts/*.sh .githooks/pre-commit`: passed.
  - `python3 -m py_compile scripts/*.py tests/*.py`: passed.
- **Functional**:
  - `make test`: passed, 20 tests.
  - isolated temporary-HOME bootstrap: first install succeeded; second reported
    Codex AGENTS, Codex hooks, and Claude hooks already current and the env-sync
    template up to date.
  - post-second-install byte comparison: all four managed targets matched their
    first-install snapshots.
  - installed-template regression asserts both agent execution and operator
    execution plus read-only verification.
  - current-host `make agent-sync-check`: expected pre-install drift only for
    Codex AGENTS; Codex and Claude hooks are already current.

Current-host bootstrap remains deliberately pending until the versioned rule is
published and integrated.

## Next steps

1. Review and publish this workspace-meta change through the new command-bundle
   workflow.
2. Install the integrated managed block on the current host.
3. Align the Saberu project rules on one `gov/git-transaction-ux` branch.
4. Resume rename on one final owner branch only after governance integration.
