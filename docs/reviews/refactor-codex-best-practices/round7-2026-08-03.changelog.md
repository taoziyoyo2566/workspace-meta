# Changelog — workspace-meta current change set

## Scope

This is the single commit-level changelog for the current workspace-meta
working tree. It consolidates the earlier review-round records so the commit
contains one historical entry while the individual plans remain available as
design evidence.

## Consolidated plans

- Codex best-practice and Hook trust hardening.
- Review scope and nearest-repository boundary hardening (W-R33).
- Universal protected-action context and trigger hardening (W-R34–W-R35).
- Git authorization routing and Codex Hook state normalization.
- Separation of workspace governance from nested project repositories.
- New VPS onboarding, activation, and recovery runbook.

## Repository changes

- Codex Hook synchronization now preserves Codex-owned `[hooks.state]` when
  Codex places it inside the managed marker, distinguishes state normalization
  from definition changes, and warns for `/hooks` trust only when the managed
  definition changed.
- Added regression coverage for state preservation, evaluator-hash changes,
  authorization action briefs, route symmetry, and safe-work exemptions.
- Added the shared Protected-Action Request Brief and routed protected Git
  modules through the canonical authorization owner.
- Hardened review scope guidance so the nearest Git root, nested repositories,
  and host-local state are classified before supplemental inspection.
- Updated workspace documentation and adapters for the
  `~/workspace/projects/<project>/` boundary.
- Added the complete [new VPS runbook](../../runbooks/new-vps.md), including
  explicit action briefs for environment probing, bootstrap, updates, Hook
  trust, and fresh-session activation.
- Kept the root reverse whitelist explicit for the review artifacts and runbook.

## Verification

Passed:

- `make test` — 28 tests passed.
- `bash -n scripts/*.sh .githooks/pre-commit`.
- `python3 -m py_compile scripts/*.py tests/*.py`.
- Bash syntax check of all fenced `bash` snippets in the runbook.
- TOML, JSON, and YAML parsing checks.
- Internal README/architecture links and reverse-whitelist checks.
- Isolated synchronizer run twice; managed output hashes were unchanged on the
  second run.
- Current read-only checks: `agent-sync-check=0`, `env-probe-check=0`,
  `core.hooksPath=.githooks`, and executable pre-commit guard.
- `origin/main` refreshed and remains identical to `HEAD`.
- Nested project roots were checked as supplemental evidence; they remain
  separate repositories and `projects/` remains ignored by workspace-meta.

## Gaps and boundaries

- Full `make bootstrap` twice was not run in the working checkout because it
  writes `.git/config` and host-local `~/.codex`/`~/.claude` files.
- Real Codex `/hooks` trust and fresh Codex/Claude session smoke tests remain
  manual host verification.
- No credentials, host authorization rules, trust state, Git staging, commit,
  push, PR, deployment, or external publication was performed.
