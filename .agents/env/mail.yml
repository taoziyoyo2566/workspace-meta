# Auto-generated by scripts/env_probe.sh — do not edit by hand.
# Refresh: scripts/env_probe.sh   Freshness gate: scripts/env_probe.sh --check
# Behavior rule: .agents/rules/environment-truth.md (probe, don't recall)
host: mail
probed_at: 2026-07-11T05:23:31Z
ttl_days: 7
capabilities:
  docker: {available: true, daemon: true, server: "29.3.1", running_count: 34}
  ansible_playbook: {available: false}
  ansible_lint: {available: false}
  yamllint: {available: false}
  python3: {available: true, version: "Python 3.13.5"}
  uv: {available: false}
  pip_audit: {available: false}
  trivy: {available: false}
  gh: {available: true, authenticated: true}
  git_push: {remote_reachable: true}
  make: {available: true, version: "GNU Make 4.4.1"}
  jq: {available: true, version: "jq-1.7"}
host_overrides:
  - "ansible not on PATH: prefer project make targets or containerized ansible; do not retry bare ansible-playbook"
  - "docker daemon up: local container-based gates (compose builds, molecule, etc.) runnable"
command_truth: "task->command mapping is per-project — see that project's testing/governance docs; this registry carries host capability facts + host-conditional hints only"
